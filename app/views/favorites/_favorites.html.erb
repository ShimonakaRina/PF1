<% if cook.favorited_by?(current_user) %>
<%= link_to cook_favorites_path(cook), method: :delete, remote: true do %> 
♥<%= cook.favorites.count %>
<% end %>
<% else %>
<%= link_to cook_favorites_path(cook), method: :post, remote: true do %>
♡<%= cook.favorites.count %>
<% end %>
<% end %>